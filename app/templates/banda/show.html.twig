{% extends 'main.html.twig' %}

{% block title %}Banda{% endblock %}

{% block body %}
    <h1>Banda</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ banda.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ banda.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('banda_index') }}">back to list</a>

    <a href="{{ path('banda_edit', {'id': banda.id}) }}">edit</a>

    {{ include('banda/_delete_form.html.twig') }}
{% endblock %}
