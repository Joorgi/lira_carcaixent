{% extends 'base.html.twig' %}

{% block title %}Pieza{% endblock %}

{% block body %}
    <h1>Pieza</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pieza.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ pieza.Nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pieza_index') }}">back to list</a>

    <a href="{{ path('pieza_edit', {'id': pieza.id}) }}">edit</a>

    {{ include('pieza/_delete_form.html.twig') }}
{% endblock %}
