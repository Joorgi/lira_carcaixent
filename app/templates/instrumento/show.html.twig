{% extends 'base.html.twig' %}

{% block title %}Instrumento{% endblock %}

{% block body %}
    <h1>Instrumento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instrumento.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ instrumento.Nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('instrumento_index') }}">back to list</a>

    <a href="{{ path('instrumento_edit', {'id': instrumento.id}) }}">edit</a>

    {{ include('instrumento/_delete_form.html.twig') }}
{% endblock %}
