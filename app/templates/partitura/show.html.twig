{% extends 'base.html.twig' %}

{% block title %}Partitura{% endblock %}

{% block body %}
    <h1>Partitura</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partitura.id }}</td>
            </tr>
            <tr>
                <th>RolInstrumento</th>
                <td>{{ partitura.rolInstrumento }}</td>
            </tr>
            <tr>
                <th>Fichero</th>
                <td>{{ partitura.fichero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partitura_index') }}">back to list</a>

    <a href="{{ path('partitura_edit', {'id': partitura.id}) }}">edit</a>

    {{ include('partitura/_delete_form.html.twig') }}
{% endblock %}
